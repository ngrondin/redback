import { RbAggregatesetDirective } from './rb-aggregateset.directive';

describe('RbAggregatesetDirective', () => {
  it('should create an instance', () => {
    const directive = new RbAggregatesetDirective();
    expect(directive).toBeTruthy();
  });
});
