import { RbHsectionDirective } from './rb-hsection.directive';

describe('RbHsectionDirective', () => {
  it('should create an instance', () => {
    const directive = new RbHsectionDirective();
    expect(directive).toBeTruthy();
  });
});
