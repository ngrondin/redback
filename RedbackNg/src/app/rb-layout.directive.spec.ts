import { RbLayoutDirective } from './rb-layout.directive';

describe('RbLayoutDirective', () => {
  it('should create an instance', () => {
    const directive = new RbLayoutDirective();
    expect(directive).toBeTruthy();
  });
});
