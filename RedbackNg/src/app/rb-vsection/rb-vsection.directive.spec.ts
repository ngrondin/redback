import { RbVsectionDirective } from './rb-vsection.directive';

describe('RbVsectionDirective', () => {
  it('should create an instance', () => {
    const directive = new RbVsectionDirective();
    expect(directive).toBeTruthy();
  });
});
