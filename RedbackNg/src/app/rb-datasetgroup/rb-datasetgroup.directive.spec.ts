import { RbDatasetgroupDirective } from './rb-datasetgroup.directive';

describe('RbDatasetgroupDirective', () => {
  it('should create an instance', () => {
    const directive = new RbDatasetgroupDirective();
    expect(directive).toBeTruthy();
  });
});
