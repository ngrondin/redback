import { RbTipDirective } from '../../rb-tip.directive';

describe('RbTipDirective', () => {
  it('should create an instance', () => {
    const directive = new RbTipDirective();
    expect(directive).toBeTruthy();
  });
});
