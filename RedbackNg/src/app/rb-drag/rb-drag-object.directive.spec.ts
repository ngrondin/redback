import { RbDragObjectDirective } from './rb-drag-object.directive';

describe('RbDragObjectDirective', () => {
  it('should create an instance', () => {
    const directive = new RbDragObjectDirective();
    expect(directive).toBeTruthy();
  });
});
