import { RbDragChangeformDirective } from './rb-drag-changeform.directive';

describe('RbDragChangeformDirective', () => {
  it('should create an instance', () => {
    const directive = new RbDragChangeformDirective();
    expect(directive).toBeTruthy();
  });
});
