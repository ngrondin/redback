import { RbViewLoaderDirective } from './rb-view-loader.directive';

describe('RbViewLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new RbViewLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
