import { RbTabSectionDirective } from './rb-tab-section.directive';

describe('RbTabSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new RbTabSectionDirective();
    expect(directive).toBeTruthy();
  });
});
