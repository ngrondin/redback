import { RbTabDirective } from './rb-tab.directive';

describe('RbTabDirective', () => {
  it('should create an instance', () => {
    const directive = new RbTabDirective();
    expect(directive).toBeTruthy();
  });
});
