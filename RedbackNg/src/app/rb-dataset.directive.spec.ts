import { RbDatasetDirective } from './rb-dataset.directive';

describe('RbDatasetDirective', () => {
  it('should create an instance', () => {
    const directive = new RbDatasetDirective();
    expect(directive).toBeTruthy();
  });
});
