import { RbFilesetDirective } from './rb-fileset.directive';

describe('RbFilesetDirective', () => {
  it('should create an instance', () => {
    const directive = new RbFilesetDirective();
    expect(directive).toBeTruthy();
  });
});
