import { RbViewDirective } from './rb-view.directive';

describe('RbViewDirective', () => {
  it('should create an instance', () => {
    const directive = new RbViewDirective();
    expect(directive).toBeTruthy();
  });
});
