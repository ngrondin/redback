import { RbListScrollDirective } from './rb-list-scroll.directive';

describe('RbListScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new RbListScrollDirective();
    expect(directive).toBeTruthy();
  });
});
