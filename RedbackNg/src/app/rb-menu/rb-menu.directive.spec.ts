import { RbMenuDirective } from './rb-menu.directive';

describe('RbMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new RbMenuDirective();
    expect(directive).toBeTruthy();
  });
});
